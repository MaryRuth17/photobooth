generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Photo {
  id        Int      @id @default(autoincrement())
  imageData String
  createdAt DateTime @default(now())
}

